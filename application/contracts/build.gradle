plugins {
    id 'java-library'
}

dependencies {
    // Minimal dependencies - this module should be lightweight
    // No AWS SDK, no framework dependencies
    api "com.fasterxml.jackson.core:jackson-databind:${project.property('jacksonVersion')}"
}
