plugins {
    id 'java-library'
}

dependencies {
    // Minimal dependencies — SPI module should be lightweight
    api "com.fasterxml.jackson.core:jackson-databind:${project.property('jacksonVersion')}"
}
