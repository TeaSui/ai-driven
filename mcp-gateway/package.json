{
  "name": "mcp-gateway",
  "version": "1.0.0",
  "description": "Unified MCP Gateway Lambda for AI-Driven project",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && cp package.json dist/ && cd dist && npm install --omit=dev && zip -r ../mcp-gateway.zip .",
    "clean": "rm -rf dist mcp-gateway.zip node_modules"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.500.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
